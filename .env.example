# Supabase config. Copy this file to `.env` and fill in real values.
# Both are *anon* (public) keys — safe to ship in the client. RLS still applies.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=eyJ...
